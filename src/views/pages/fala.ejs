<%- contentFor('body') %>
<div class="container-fala">
    <h1 class="titulo">Peça sua música </h1>
    <p>Se você tiver sorte, sua música vai ser escolhida para ser tocada ao vivo</p>
    <div class="campo">
        <label>Seu Nome</label>
        <div>
            <input type="text">
        </div>
    </div>
    <div class="campo">
        <label>Seu Música</label>
        <div>
            <input type="text">
        </div>
    </div>
    <div class="campo">
        <label>Mande uma mensagem</label>
        <div>
            <textarea></textarea>
        </div>
    </div>
    <div class="c-btn mt-4">
        <button>
            Enviar
        </button>
    </div>
</div>
<style>
    .container-fala{
        color: var(--font-color-clara);
    }
    .campo{
        margin: 1.2rem 0;
        width: 50vw;
    }
    .campo label{
        font-weight: bold;
    }
    .campo input:focus, .campo textarea:focus,
    .campo input, .campo textarea{
        border: none;
        outline:none;
        border-bottom: 1px solid var(--color-font-sem-destaque);
        transition: all 200ms ease-in;
    }
    .campo input:focus, .campo textarea:focus{
        transition: all 200ms ease-in;
        border-bottom: 1px solid #fff;
    }
    .campo input, .campo textarea{
        transition: all 200ms ease-in;

        width: 100%;
        background-color: unset;
        color: var(--font-color-clara);
    }
    .c-btn button{
        padding: 0.3em 4rem;
        border: none;
        background: var(--gradiente-padaro-invertido);
        color: #fff;
    }
    .c-btn button:hover{
        filter: contrast(2);
    }
</style>